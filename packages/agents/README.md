# Agent Package

Future home for provider-independent agent contracts, schemas, prompts, routing policies, playbooks and deterministic post-processing.

Model/provider clients should be adapters, not embedded business rules.
